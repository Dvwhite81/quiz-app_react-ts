interface NavBarProps {

}

const NavBar = ({ }: NavBarProps) => {
  return (
    <div id="navbar">
      <img id="navbar-logo" src="/logo-transparent-3.png" alt="trivia hero logo" />
    </div>
  );
};

export default NavBar;
